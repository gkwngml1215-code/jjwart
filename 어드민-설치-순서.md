# 관리 도구 v2.0 설치 순서 (Cloudflare Pages ← GitHub 연결)

관리 도구를 **GitHub 저장소에 연결된 Cloudflare Pages**로 올리는 절차입니다.
예전처럼 폴더를 끌어다 놓는 방식(Direct Upload)은 쓰지 않습니다.
이제는 관리 도구를 고치면 GitHub에 올리는 것만으로 1~2분 뒤 자동으로 바뀝니다.

전부 무료이고, 처음 한 번만 하면 됩니다. **1단계부터 순서대로** 해 주세요.

> 만드는 방법과 지켜야 할 규칙은 `어드민-제작-가이드.md`에 있습니다.

---

## 0단계 — 옛 관리 도구(v1.0)는 쓰지 마세요

지금 `hoo-admin.pages.dev`에 올라가 있는 관리 도구는 v1.0입니다.
v1.0에서 [홈페이지에 바로 반영]을 누르면 **다른 글을 덮어쓰거나 목록에서 지울 수 있습니다.**
아래 1단계에서 옛 접근 키를 지우면 v1.0은 더 이상 홈페이지에 글을 올릴 수 없게 됩니다.

---

## 1단계 — GitHub 접근 키 새로 만들고, 옛 키 지우기

1. https://github.com/settings/personal-access-tokens 접속
2. 목록에 있는 **옛 키(`후한의원 어드민`)** 를 눌러 **[Delete]** 합니다
   → 이제 옛 관리 도구(v1.0)는 글을 올리지 못합니다
3. 오른쪽 위 **[Generate new token]** 클릭 후 아래처럼 입력

   | 항목 | 넣을 값 |
   |---|---|
   | Token name | `후한의원 전주점 관리도구 v2` |
   | Expiration | **No expiration** (만료 없음) |
   | Repository access | **Only select repositories** → `jjwart` 선택 |
   | Permissions → Repository permissions → **Contents** | **Read and write** |

   > 나머지 권한은 손대지 마세요.

4. **[Generate token]** → `github_pat_`로 시작하는 문자열을 메모장에 복사해 둡니다
   (이 화면을 벗어나면 다시 볼 수 없습니다)

---

## 2단계 — Cloudflare Pages 를 GitHub 에 연결해서 만들기

1. https://dash.cloudflare.com 로그인 → 왼쪽 **Workers & Pages**
2. **[Create]** → 위쪽 **Pages** 탭 → **[Connect to Git]**
3. **GitHub** 선택 → 처음이면 GitHub 화면이 뜹니다
   - **Only select repositories** → `jjwart` 만 선택 → **[Install & Authorize]**
4. 저장소 목록에서 **`jjwart`** 선택 → **[Begin setup]**
5. 아래처럼 입력합니다 (**가장 중요한 단계**)

   | 항목 | 넣을 값 |
   |---|---|
   | Project name | `jjwart-admin` (주소가 `jjwart-admin.pages.dev`가 됩니다) |
   | Production branch | **`admin`** ← 반드시 admin (main 아님) |
   | Framework preset | **None** |
   | Build command | (비워둠) |
   | Build output directory | **`/`** |

6. **[Save and Deploy]** → 1분쯤 기다리면 주소가 생깁니다

> `main` 브랜치는 홈페이지(jjhoowart.co.kr)입니다. 여기서 Production branch를 main으로 고르면
> 관리 도구 대신 홈페이지가 올라가 버리니 꼭 **admin**으로 해 주세요.

---

## 3단계 — 미리보기 배포 끄기

관리 도구로 글을 발행하면 `main` 브랜치에 올라갑니다. 이때 Cloudflare가 main을 "미리보기"로
따로 배포하지 않게 꺼 둡니다.

1. 방금 만든 프로젝트 → **Settings** → **Builds & deployments** (또는 **Build**)
2. **Branch control** 항목 → **[Edit]**
3. **Production branch**: `admin` 인지 확인, **Automatic deployments** 켜짐
4. **Preview branch**: **None** 선택 → **[Save]**

---

## 4단계 — 아이디·비밀번호 정하기

1. 프로젝트 → **Settings** → **Variables and Secrets** → **[Add]** 로 2개 추가

   | 이름 | 값 | 종류 |
   |---|---|---|
   | `ADMIN_USER` | 원하는 아이디 | **Secret** |
   | `ADMIN_PASS` | 원하는 비밀번호 | **Secret** |

   > 이름은 대문자로 정확히. 적용 대상은 **Production**.

2. **[Save]** → **Deployments** 탭 → 최신 배포 오른쪽 **[...]** → **[Retry deployment]**
   (환경 변수는 다시 배포해야 적용됩니다)

---

## 5단계 — 잠금·연결 확인

1. 시크릿 창(Ctrl+Shift+N)으로 `https://jjwart-admin.pages.dev` 접속
2. **아이디·비밀번호 입력창이 뜨면 잠금 성공**
3. 로그인 → 화면 맨 아래 **[🔑 GitHub 연결]** → 1단계의 새 키 붙여넣기
4. "8. 원장 컬럼" 칸에 **「홈페이지에 올라간 컬럼 9개」** 가 보이면 끝입니다

| 이런 화면이 나오면 | 원인 | 해결 |
|---|---|---|
| 입력창 없이 바로 열림 | `_worker.js`가 적용 안 됨 | Build output directory가 `/`인지 확인 후 재배포 |
| `설정 오류: ... ADMIN_USER ...` | 환경 변수 이름이 틀림 | 4단계 표와 한 글자씩 대조 |
| 맞는 비번인데 계속 물어봄 | 재배포를 안 함 | 4단계 2번(Retry deployment) |
| 홈페이지가 보임 | Production branch가 main | 2단계 5번을 admin으로 |
| 「목록을 불러오지 못했습니다」 | 키 권한 부족 | 1단계 권한(Contents: Read and write, jjwart) 확인 |

---

## 6단계 — 옛 Direct Upload 프로젝트 지우기

새 관리 도구가 잘 되는 것을 확인한 뒤에:

1. **Workers & Pages** → 옛 프로젝트 **`hoo-admin`** → **Settings** → 맨 아래 **[Delete project]**

> 주소를 예전 그대로(`hoo-admin.pages.dev`) 쓰고 싶으면, 옛 프로젝트를 **먼저** 지운 뒤
> 2단계의 Project name을 `hoo-admin`으로 하면 됩니다. (그 사이 몇 분은 관리 도구를 쓸 수 없습니다)

---

## 이제 이렇게 씁니다

1. 어디서든 관리 도구 주소 접속 → 아이디·비밀번호
2. **8. 원장 컬럼** → 고칠 글 고르기 또는 **[+ 새 컬럼]**
   - 목록은 항상 **홈페이지에 올라가 있는 글** 기준입니다. 다른 PC·폰·시크릿 창에서도 똑같이 보입니다.
   - 새 글 파일명은 홈페이지에 있는 파일과 겹치지 않게 자동으로 정해집니다.
3. 다 쓰면 맨 아래 **[발행 전 확인]**
4. 확인 화면에서 **발행 후 글 개수**와 **추가·수정·유지·삭제** 목록을 보고 **[발행하기]**
   - 숫자가 이상하면 [취소] — 홈페이지는 아무것도 바뀌지 않습니다.
5. 1~2분 뒤 jjhoowart.co.kr 에 반영됩니다

**글 삭제**는 글을 고른 뒤 **[이 글 삭제]** → 확인창 → 확인 화면의 **[삭제하고 발행]** 까지 눌러야만 됩니다.

**쓰다가 창을 닫아도** 쓰던 내용은 그 기기 브라우저에 임시저장되어 있습니다. (다른 기기로는 넘어가지 않으니, 옮기려면 먼저 발행해 주세요)

---

## 관리 도구를 고쳤을 때

- 관리 도구 파일은 `jjwart` 저장소의 **`admin` 브랜치**에 있습니다.
- 고친 내용을 admin 브랜치에 커밋·푸시하면 Cloudflare가 **1~2분 뒤 자동으로 다시 배포**합니다. 끌어다 놓을 필요가 없습니다.
- 배포 기록과 되돌리기: Cloudflare 프로젝트 → **Deployments** 탭 (예전 배포로 즉시 되돌릴 수 있음)
- 고치기 전후에 `어드민-제작-가이드.md`의 검증 두 가지를 꼭 실행합니다.

**`_worker.js`는 절대 지우거나 이름을 바꾸지 마세요.** 이 파일이 아이디·비밀번호 잠금입니다.

**폰·노트북을 잃어버렸다면** GitHub에서 그 키를 지우세요 (1단계 2번과 같은 방법). 그러면 그 기기에서는 글을 올릴 수 없습니다.
