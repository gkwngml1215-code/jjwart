# 어드민(관리 도구) 제작 가이드 (전주점·강남점 공용)

후한의원 홈페이지 관리 도구를 만들거나 고칠 때 따르는 기준입니다.
전주점·강남점 관리 도구는 **같은 코드**를 쓰고, 지점마다 다른 값은 `site-config.js` 한 파일에만 있습니다.

---

## 어드민 도구 제작 시 반드시 지켜야 할 것

1. **병합 방식으로 발행한다.**
   글/컬럼 목록을 발행할 때는 기존 목록을 통째로 지우고 새로 채우는 방식(덮어쓰기)이 아니라, 각 글의 파일명(slug)을 기준으로 "있으면 그 항목만 수정, 없으면 새로 추가"하는 방식(병합)으로 구현한다. 목록 파일에서 변경 전 내용을 먼저 읽어온 뒤, 지금 작성 중인 글만 반영하고 나머지 기존 글은 그대로 보존해야 한다.

2. **진짜 원본은 배포된 사이트(저장소)다.**
   글 목록의 "진짜 원본"은 이 도구를 여는 브라우저의 localStorage가 아니라, 실제 배포되어 있는 사이트(또는 저장소)여야 한다. 발행 버튼을 누르면 매번 배포된 목록을 먼저 불러와서 그 위에 병합해야 하고, 브라우저 저장공간에만 있는 값을 기준으로 전체 목록을 다시 만들면 안 된다. 즉, 다른 기기·다른 브라우저·시크릿모드에서 열어도 이전에 올린 글들이 사라지면 안 된다.

3. **새 글 파일명은 배포된 파일과 절대 겹치지 않게 정한다.**
   새 글을 추가할 때 파일명(slug)을 자동으로 만든다면, 이미 존재하는 파일명과 절대 겹치지 않도록 실제 배포된 파일 목록을 확인하고 번호를 매긴다. 단순히 "로컬에 몇 개 있으니 다음 번호"로 정하면 안 된다.

4. **삭제는 사용자가 [삭제]를 눌렀을 때만 일어난다.**
   글을 삭제하는 기능은 사용자가 명확하게 "삭제" 버튼을 눌렀을 때만 실제 삭제로 이어지게 한다. 자동으로 목록이 줄어들거나 빠지는 일이 없어야 한다.

5. **발행 전에 확인 단계를 보여준다.**
   발행하기 전에 "지금 몇 개 글이 목록에 반영될 예정인지, 새로 추가/수정/유지되는 항목이 뭔지" 미리 보여주는 확인 단계를 넣는다.

6. **소스는 GitHub에서 관리하고, Cloudflare Pages는 Git으로 연결한다.**
   이 도구의 소스코드는 GitHub 저장소에 커밋해서 관리하고, Cloudflare Pages는 그 저장소에 연동(Git 연결)해서 배포한다. 파일을 직접 드래그해서 올리는 방식(Direct Upload)은 나중에 수정하거나 버그를 고치기 어려우니 쓰지 않는다.

7. **관리 도구를 고치면 모든 지점에 한꺼번에 적용한다.**
   전주점·강남점, 그리고 앞으로 생기는 모든 지점의 관리 도구는 같은 코드를 쓴다. 버그를 고치거나 기능을 더할 때는 한 지점만 고치지 말고, 공용 파일을 모든 지점 폴더에 똑같이 복사하고 지점마다 검증을 통과시킨 뒤 함께 올린다. (같은 코드는 같은 버그를 가진다 — v1 사고가 두 지점에서 똑같이 일어났다.)

---

## 이 규칙이 생긴 이유 — v1.0 사고 기록 (2026-09-08 ~ 09-11)

v1.0 관리 도구는 [홈페이지에 바로 반영]을 누르면 그 브라우저의 localStorage에 들어 있는 글만으로
`columns.html` 목록 **전체**와 **모든** 글 파일을 다시 썼고, 새 글 파일명을 `column-(이 브라우저의 글 개수 + 1)`로 정했습니다.

그래서 새 브라우저(기본 글 3개만 있는 상태)에서 새 글을 만들면 파일명이 `column-04`가 되어 이미 올라가 있던
`column-04`를 덮어썼고, 그 브라우저에 없던 글은 목록에서 빠졌습니다. 전주점 컬럼 5편, 강남점 3편이
"새 컬럼 제목"이라는 빈 글로 바뀌었고, git 기록에서 되살렸습니다 (저장소 태그 `v1.0`).

---

## v2.1에서 찾아 고친 약점 8가지

v2.0을 만든 뒤 "새 글을 쓸 때 또 사고가 날 수 있는 곳"을 스스로 점검해 찾은 것들입니다.

| # | 약점 (v2.0) | 무슨 일이 생기나 | v2.1 대책 |
|---|---|---|---|
| 1 | 임시저장본을 한 덩어리로 저장 | 탭을 두 개 열면 서로 덮어씀 (v1과 같은 "통째로 덮어쓰기" 구조) | 글마다 따로 저장(`…_draft_v2:<글>`). 같은 글을 다른 탭에서 고치면 알려 주고 [다른 탭 내용 불러오기] 제공 |
| 2 | 올린 직후 연결이 끊기면 "실패"로 안내 | 실제로는 올라갔는데 다시 누르면 **같은 글이 두 번** (전주 "아이한테 옮을 수도"가 실제로 두 번 올라갔었음) | 저장소를 다시 읽어 실제 결과를 확인한 뒤 "완료"로 처리. 새 글 제목이 이미 있는 글과 같으면 체크해야만 발행 |
| 3 | 사진 존재 확인 없음 | 없는 경로로 발행되어 사진이 깨짐 (강남 "성지" 글) | 대표·본문·`<img>` 사진이 저장소에 있는지 확인, 없으면 어떤 사진인지 알려 주고 멈춤 |
| 4 | 본문 HTML 검사 없음 | 깨진 태그, 여는 태그 없는 `</div>`, 마크다운·```가 그대로 올라감 | 본문을 고칠 때마다 형식 검사 표시, 발행 전 차단. [본문 자동 정리]로 메모장 글·마크다운을 문단/소제목/목록으로 변환 |
| 5 | [+ 새 컬럼]만 눌러도 빈 임시저장 | "새 글 · (제목 없음)"이 계속 쌓임 | 무언가 쓰기 전에는 저장하지 않고, 남아 있는 빈 것은 시작할 때 정리 |
| 6 | 오류 문구가 암호 같음 | "GitHub 요청 실패 (401)" | 키 만료·권한 부족·한도 초과·인터넷 끊김을 사람 말로 안내 |
| 7 | 발행 전 미리보기 없음 | 올리고 나서야 모양을 봄 | [미리보기] 탭에 실제 페이지 모양 표시 (사진은 저장소에서 바로 읽음) |
| 8 | 다른 경로로 목록이 망가져도 모름 | 손으로 올리거나 옛 도구를 쓰면 발견 못 함 | 홈페이지 저장소에 GitHub Actions 자동 점검: 올릴 때마다 + 매일 아침. 목록·페이지·사진·sitemap·llms 대조, **글 개수가 줄었는데 [삭제] 발행이 아니면 실패** → 메일 알림 |

---

## v2.x 가 규칙을 지키는 방법

| 규칙 | 구현 위치 | 방법 |
|---|---|---|
| 1. 병합 | `columns-core.js` → `planPublish()` | 저장소의 `columns/index.json`을 읽고, 편집 중인 글 1개만 slug 기준으로 바꾸거나 끝에 붙인다. 다른 글의 페이지 파일은 건드리지 않는다. |
| 1. 안전장치 | `planPublish()` 끝부분 | [삭제]로 고른 글 말고 한 개라도 빠지거나, 파일명이 겹치거나, 개수가 안 맞으면 발행을 멈춘다. |
| 2. 원본은 저장소 | `github-publish.js` → `ghSnapshot()` | 발행할 때마다 저장소 최신 커밋을 읽는다. `index.json`이 없거나 깨졌으면 목록을 새로 만들지 않고 멈춘다. 브라우저에는 "발행 전 임시저장본"만 둔다. |
| 2. 동시 발행 | `ghCommit()` | 바뀌는 파일 전부를 **커밋 1개**로 올리고, 브랜치 갱신은 `force:false`. 그 사이 다른 곳에서 발행했으면 거절되고 최신본으로 다시 계산해 한 번 더 확인받는다. |
| 3. 파일명 | `collectSlugs()` + `nextSlug()` | 저장소에 실제로 있는 `columns/*.html`, `columns/data/*.json`, 목록의 파일명 중 가장 큰 번호 + 1. 발행 순간에 다시 확인해서 겹치면 다음 번호로 바꿔 올린다(확인 화면에 표시). `index`·`data`는 쓸 수 없다. |
| 4. 삭제 | `publishDelete()` | [이 글 삭제] → 확인창 → 확인 화면에서 [삭제하고 발행]을 눌러야만 지워진다. |
| 5. 확인 단계 | `showPublishConfirm()` | 발행 후 목록 개수, 추가·수정·유지·삭제 개수와 제목, 바뀌는 파일 목록, 주의사항(체크 필요 항목 포함)을 보여준다. |
| 6. Git 배포 | `어드민-설치-순서.md` | 소스는 홈페이지 저장소의 `admin` 브랜치, Cloudflare Pages가 이 브랜치에 Git으로 연결. **admin 브랜치에 푸시하면 곧 배포된다.** |
| 7. 모든 지점 함께 | 이 문서 아래 "고치는 순서" | 공용 파일을 모든 지점 폴더에 복사, 지점마다 검증, 함께 커밋. |

그 밖에:
- **글 원본 값 보관**: 글마다 `columns/data/<파일명>.json`에 편집용 원본 값을 둔다. 어느 기기에서 열어도 올라간 글을 불러와 이어서 고칠 수 있다.
- **페이지에 들어가는 병원 정보**(이름·전화·주소·카카오·저작권)는 브라우저 설정값이 아니라 `site-config.js`의 `SITE`에서 가져온다. 기기마다 다른 페이지가 만들어지지 않게 하기 위해서다.
- **다른 곳에서 먼저 수정된 글**: 편집을 시작한 뒤 다른 기기에서 같은 글을 발행했다면, 확인 화면에서 체크해야만 발행된다.

---

## 파일 구성

관리 도구 (저장소 `admin` 브랜치 = 내 PC `hoo-admin` 폴더):

```
site-config.js      ← ★ 지점마다 다른 값 (도메인·병원 이름·원장·전화·주소·카카오·저장소·저장 키 이름, 1~7번 SEO 기본값)
index.html          ← 화면 (공용)
columns-core.js     ← 병합·파일명·페이지 생성·본문 검사·자동 정리 (공용, 화면과 무관한 순수 로직, node에서도 실행됨)
github-publish.js   ← GitHub 읽기·쓰기, 발행 전 확인 화면, 결과 확인 (공용)
_worker.js          ← 아이디·비밀번호 잠금 (지우면 잠금이 풀림)
tests/columns-core.test.js  ← 규칙 검증 (공용)
tests/e2e-admin.js          ← 크롬 화면 검증 (공용)
```

홈페이지 (저장소 `main` 브랜치):

```
columns/index.json          ← 컬럼 목록 원본 (발행할 때 이 파일 기준으로 병합)
columns/data/column-XX.json ← 글마다 편집용 원본 값
columns/column-XX.html      ← 실제 글 페이지 (검색엔진이 읽는 파일)
columns.html                ← 목록 페이지. COLUMNS:START ~ END 사이만 자동으로 바뀜
sitemap.xml                 ← 컬럼 주소만 자동으로 바뀜
llms.txt                    ← "## 원장 컬럼" 구역의 컬럼 줄만 자동으로 바뀜
.github/workflows/check-columns.yml   ← 자동 점검 (올릴 때마다 + 매일 06:00)
.github/scripts/check-columns.js      ← 점검 내용
```

| 지점 | 홈페이지 저장소 | 관리 도구 주소 | 내 PC 폴더 |
|---|---|---|---|
| 전주점 | `gkwngml1215-code/jjwart` (jjhoowart.co.kr) | jjwart.pages.dev | `hoo-admin` |
| 강남점 | `gkwngml1215-code/gnwart` (gnhoowart.co.kr) | gnwart-admin.pages.dev | `hoo-gangnam-admin` |

---

## 관리 도구를 고치는 순서 (모든 지점 함께)

1. 한 지점 폴더(예: `hoo-admin`)에서 공용 파일을 고친다. 지점 값은 절대 공용 파일에 적지 않는다 — `site-config.js`의 `SITE`에서 읽는다.
2. 그 폴더에서 검증 두 가지를 통과시킨다.
   ```bash
   node tests/columns-core.test.js
   node tests/e2e-admin.js "<이 지점 홈페이지 저장소 폴더>" "C:/Program Files/Google/Chrome/Application/chrome.exe" "<임시 폴더>"
   ```
3. 공용 파일 5개(`index.html`, `columns-core.js`, `github-publish.js`, `tests/*`)를 **다른 모든 지점 폴더에 똑같이 복사**한다. (`site-config.js`·`_worker.js`는 복사하지 않는다)
4. 지점마다 검증 두 가지를 다시 통과시킨다. (화면 검증은 같은 포트를 쓰므로 한 번에 한 지점씩)
5. 지점마다 `admin` 브랜치에 커밋·푸시한다. 푸시하면 Cloudflare가 1~2분 뒤 자동 배포한다.
6. 이 문서의 버전 기록을 고치고, 새 버전 태그(`admin-v2.x`)를 모든 지점 저장소에 단다.

새 지점을 만들 때: 공용 파일 5개 + `_worker.js`를 복사하고, `site-config.js`만 새로 쓴다. 홈페이지 저장소에는 `columns/index.json`·`columns/data/`·`.github/`를 먼저 만들어 둔다.

---

## 화면 검증이 확인하는 것

새 브라우저에서 새 글 발행(기존 글 유지) · 옛 v1 저장값이 남은 브라우저에서 이미 있는 파일명 입력 시 자동 변경 · 기존 글 수정 · 동시 발행 충돌 · 취소 · 발행 전 검사 · 삭제 · **연결 끊김 뒤 결과 확인(중복 없음) · 같은 제목 새 글 체크 · 없는 사진 차단 · 본문 자동 정리와 깨진 본문 차단 · 빈 새 글 미저장 · 탭 두 개 · 미리보기** · 다른 지점 저장소로 요청이 새지 않는지.

GitHub는 가짜(메모리 저장소)로 대체되므로 실제 홈페이지는 바뀌지 않습니다.

---

## 버전 기록

| 버전 | 태그 | 내용 |
|---|---|---|
| v1.0 | 홈페이지 `v1.0`, 관리 도구 `admin-v1.0` | 브라우저 저장값 기준 덮어쓰기 방식 (위 사고의 원인). 홈페이지는 사라진 컬럼을 복원한 상태. |
| v2.0 | 홈페이지 `v2.0`, 관리 도구 `admin-v2.0` | 저장소 원본(`columns/index.json`) 기준 병합 발행, 발행 전 확인 화면, 파일명 충돌 방지, 명시적 삭제, 커밋 1개로 발행, 동시 발행 보호, Cloudflare Git 연결. |
| v2.1 | 관리 도구 `admin-v2.1` | 공용 코드 + `site-config.js` 분리. 약점 8가지 보강: 탭별 임시저장, 연결 끊김 결과 확인·중복 발행 방지, 사진 존재 확인, 본문 검사·자동 정리, 빈 임시저장 방지, 알기 쉬운 오류, 미리보기, 홈페이지 자동 점검(Actions). |
